## Быстрый старт

```bash
git clone https://github.com/username/kittygram.git
cd kittygram

# Копируем пример переменных
cp .env.example .env

# Если хочешь поменять пароль/секретный ключ — отредактируй .env

# Запускаем
docker compose up --build